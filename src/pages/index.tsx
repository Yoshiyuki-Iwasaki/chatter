import TodoList from "../components/chat/ChatList";
import Layout from "../components/Layout";

const Home = () => {

  return (
    <Layout>
      <TodoList />
    </Layout>
  );
}

export default Home;
