export type SidebarType = {
  uid: any;
  photoURL: string;
  displayName: string;
};

export type SidebarItemType = {
  id: any;
  title: string;
};

export type SidebarListType = {
  currentUserId?: number;
};
